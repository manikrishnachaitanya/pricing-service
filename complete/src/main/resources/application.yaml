spring:
  application:
    name: car-services
  profiles:
    active: dev
server:
  port: 8081
  servlet:
    context-path: /api
logging:
  level:
    root: TRACE
    org:
      apache:
        tomcat: INFO
    com.example.myapp: debug